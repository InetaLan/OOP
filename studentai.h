#pragma once

#include <string>
#include <vector>

using std::string;
using std::vector;


struct studentai {
    string Vard, Pav;
    vector<int> paz = { 0 };
    int egz;
    float GP = 0;
};